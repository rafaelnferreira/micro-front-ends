package com.micro;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeITFrontEndResource extends TestFrontEndResource {

    // Execute the same tests but in native mode.
}